self.addEventListener('fetch', function(e){
    //console.log(e.request.url);

    // if(e.request)
    // e.respondWith(
    //     fetch(e.request)
    //         .then((response) => {
    //             if (!response.ok) console.log(e.request);
    //         })
    //         .catch((err) => {
    //             console.log("offline?");

    //         })
    // )
});